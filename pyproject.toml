[project]
name = "logseq-vrig"
version = "1.0.0"
description = "Local guardrails for AI-assisted Logseq OG graphs"
requires-python = ">=3.11"

[project.scripts]
vault-rig = "vault_rig.__main__:main"

[tool.unittest]
start-directory = "tests"
