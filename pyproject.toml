[project]
name = "logseq-vrig"
version = "0.1.0"
description = "Portable tooling for a managed Logseq vault area"
requires-python = ">=3.11"

[project.scripts]
vault-rig = "vault_rig.__main__:main"

[tool.unittest]
start-directory = "tests"
