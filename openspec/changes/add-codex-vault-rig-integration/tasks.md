## 1. Current Codex Contract

- [ ] 1.1 Recheck the installed Codex release and official AGENTS.md, Skills, hooks, sandbox, and approval documentation, then record the exact supported project file locations and keys used by the payload.

## 2. Progressive Context

- [ ] 2.1 Create the concise managed AGENTS.md region for source of truth, retrieval routing, edit invariants, and completion checks, then verify it stays bounded and contains no procedural duplication.
- [ ] 2.2 Create one .agents/skills/vault-rig/SKILL.md for retrieval and safe-edit procedures, then verify Codex discovers it from the repository and its commands reference the stable CLI.

## 3. Lifecycle Hooks

- [ ] 3.1 Implement the SessionStart hook to emit bounded vault, Git, capability, and integrity context, then verify startup, resume, and unavailable-command fixtures never dump graph content.
- [ ] 3.2 Implement the Stop hook using current JSON input and stop_hook_active behavior, then verify pass, first-fail continuation, second-fail termination, and unchanged-canonical-file paths.

## 4. Authority and Installation

- [ ] 4.1 Add the supported project configuration for workspace-scoped writes, on-request approvals, and no default network use, then verify the installed Codex release accepts the configuration.
- [ ] 4.2 Add the Codex payload to lifecycle ownership and uninstall behavior, then verify pre-existing root instructions survive install, update, conflict, and uninstall.
- [ ] 4.3 Run a manual Codex smoke test with Logseq closed, then verify retrieval, canonical edit checking, bounded retry, and routine single-agent completion behave as specified.
